import { copyFile, mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";

const root=process.cwd(),gameBuild=process.env.PAL_GAME_BUILD||"24467282",source=process.env.PAL_DATA_SOURCE||path.join(root,"private","palcalc-source"),extracted=process.env.PAL_EXTRACTED_SOURCE||path.join(root,"private","extracted",`build-${gameBuild}`);
const db=JSON.parse(await readFile(path.join(source,"PalCalc.Model","db.json"),"utf8"));
const localeMap={de:"de-DE",en:"en-US","es-MX":"es-419",es:"es-ES",fr:"fr-FR",id:"id-ID",it:"it-IT",ko:"ko-KR",pl:"pl-PL","pt-BR":"pt-BR",ru:"ru-RU",th:"th-TH",tr:"tr-TR",vi:"vi-VN","zh-Hans":"zh-CN","zh-Hant":"zh-TW",ja:"ja-JP"};
const localized=value=>Object.fromEntries(Object.entries(localeMap).map(([from,to])=>[to,value?.[from]||value?.en||""]));
const [officialNames,officialDescriptions,palDescriptions,palParameters]=await Promise.all(["skill-names.raw.json","skill-descriptions.raw.json","pal-long-descriptions.raw.json","pal-parameters.raw.json"].map(file=>readFile(path.join(extracted,file),"utf8").then(JSON.parse)));
const official=(tables,key)=>Object.fromEntries(Object.entries(localeMap).map(([from,to])=>[to,tables[from]?.[key]||tables.en?.[key]||""]));
const elements=db.Elements.map(element=>({id:element.InternalName,names:localized(element.LocalizedNames),icon:`/assets/elements/${element.InternalName}.png`}));
const activeSkills=db.ActiveSkills.map(skill=>({id:skill.InternalName,names:official(officialNames,`ACTION_SKILL_${skill.InternalName}`),descriptions:official(officialDescriptions,`ACTION_SKILL_${skill.InternalName}`),elementId:skill.ElementInternalName,power:skill.Power,cooldown:skill.CooldownSeconds,canInherit:skill.CanInherit,hasSkillFruit:skill.HasSkillFruit})).filter(skill=>Object.values(skill.names).some(name=>name.trim())).sort((a,b)=>a.id.localeCompare(b.id));
const passiveSkills=db.PassiveSkills.filter(skill=>skill.IsStandardPassiveSkill&&skill.Description&&skill.LocalizedDescriptions).map(skill=>({id:skill.InternalName,names:official(officialNames,`PASSIVE_${skill.InternalName}`),descriptions:localized(skill.LocalizedDescriptions),rank:skill.Rank,randomInheritanceAllowed:skill.RandomInheritanceAllowed,surgeryCost:skill.SurgeryCost,surgeryRequiredItem:skill.SurgeryRequiredItem})).sort((a,b)=>a.id.localeCompare(b.id));
const partnerSkills=db.Pals.map(pal=>{const row=Object.values(palParameters).find(entry=>entry.BPClass===pal.InternalName&&entry.ZukanIndex===pal.Id.PalDexNo&&Boolean(entry.ZukanIndexSuffix)===Boolean(pal.Id.IsVariant))||{};const nameKey=row.OverridePartnerSkillNameTextID&&row.OverridePartnerSkillNameTextID!=="None"?row.OverridePartnerSkillNameTextID:`PARTNERSKILL_${pal.InternalName}`;return {id:pal.InternalName,palId:pal.InternalName,names:official(officialNames,nameKey),palDescriptions:official(palDescriptions,`PAL_LONG_DESC_${pal.InternalName}`)}}).sort((a,b)=>a.id.localeCompare(b.id));
if(elements.length!==9||activeSkills.length<300||passiveSkills.length<100)throw new Error("Unexpected skill dataset counts");
await mkdir(path.join(root,"public","data"),{recursive:true});
await writeFile(path.join(root,"public","data","skills.json"),JSON.stringify({meta:{schema:2,gameBuild,sourceDbVersion:db.Version,generatedAt:new Date().toISOString(),verification:"statistics community-normalized; names and descriptions from installed game files",elementCount:elements.length,activeSkillCount:activeSkills.length,passiveSkillCount:passiveSkills.length,partnerSkillCount:partnerSkills.length},elements,activeSkills,passiveSkills,partnerSkills}));
await mkdir(path.join(root,"public","assets","elements"),{recursive:true});
for(const element of elements)await copyFile(path.join(source,"PalCalc.UI","Resources","Elements",`${element.id}.png`),path.join(root,"public","assets","elements",`${element.id}.png`));
await mkdir(path.join(root,"public","assets","passive-ranks"),{recursive:true});
for(const rank of [-3,-2,-1,1,2,3,4,5])await copyFile(path.join(source,"PalCalc.UI","Resources","TraitRank",`Passive_${rank>0?"Positive":"Negative"}_${Math.abs(rank)}_icon.png`),path.join(root,"public","assets","passive-ranks",`${rank}.png`));
console.log(`Imported ${elements.length} elements, ${activeSkills.length} active, ${passiveSkills.length} passive and ${partnerSkills.length} partner skills.`);
