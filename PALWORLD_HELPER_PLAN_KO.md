# Palworld Helper 제작 작업 계획

> 문서 기준일: 2026-07-26  
> 프로젝트 상태: 신규 구축(현재 저장소에 구현물 없음)  
> 출시 원칙: 정확성 우선, 정적 사이트 우선, 게임 공식 17개 인터페이스 언어 동시 지원

## 1. 제품 목표

Palworld Helper는 단순 위키가 아니라, 플레이 중 반복적으로 생기는 질문을 몇 번의 선택만으로 해결하는 도구형 사이트를 목표로 한다.

- 어디에서 무엇을 얻는가 → 다층 인터랙티브 지도
- 어떤 팰을 잡고/키우고/배치해야 하는가 → 팰 데이터베이스와 비교
- 보유 팰로 원하는 팰과 패시브를 어떻게 만드는가 → 교배 경로 계산기
- 필요한 자원과 작업 인력을 얼마나 준비해야 하는가 → 제작·생산·기지 계산기
- 포획 성공 가능성과 준비물이 얼마나 필요한가 → 포획 계산기
- 서버 설정을 어떻게 안전하게 바꾸는가 → 공식 설정 기반 생성기와 검증기
- 다음에 무엇을 해야 하는가 → 진행도별 체크리스트와 상황별 가이드

사이트가 답을 모를 때는 값을 추정하지 않는다. `알 수 없음`, `현재 버전에서 미검증`, `해당 없음`, 숫자 `0`을 서로 다른 상태로 다룬다.

## 2. 핵심 제품 원칙

1. 게임 설치 파일에서 재현 가능하게 추출한 데이터가 최우선이다.
2. 공식 사이트·공식 패치 노트·공식 서버 문서·공식 스토어 자료로 보완한다.
3. palworld.gg와 paldb.cc 등 커뮤니티 사이트는 기능 벤치마킹과 누락 탐지에 사용하며, 사실값은 그대로 복사하지 않고 게임 파일 또는 공식 자료로 재검증한다.
4. 모든 숫자에는 `gameVersion`, 데이터 생성일, 검증 상태를 내부적으로 연결한다.
5. 패치 후 데이터가 오래되면 페이지 상단에 명확한 경고를 표시하고 계산 결과를 “최신”이라고 주장하지 않는다.
6. 콘텐츠는 검색 가능하고 공유 가능해야 하며, 모바일에서 핵심 기능을 동일하게 사용할 수 있어야 한다.
7. 로그인 없이 동작하고, 즐겨찾기·보유 팰·체크 상태는 기본적으로 브라우저에만 저장한다.
8. 지도·계산기·검색 입력값 같은 자유 텍스트는 Analytics로 전송하지 않는다.

## 3. 조사 결과와 제품 기회

### 3.1 확인된 공식 범위

- Steam 공식 상점은 17개 인터페이스/자막 언어를 표시한다.
- 공식 Palworld Server Guide는 현재 1.0.0 문서와 서버 실행 인자, INI 설정, REST API, RCON, 백업, PvP, 모드 설치 정보를 제공한다.
- 공식 설정 문서는 `PalWorldSettings.ini`의 위치와 성능·서버 관리·게임 밸런스 파라미터를 설명한다.
- 공식 서버 문서상 일부 필드는 폐기되거나 향후용일 수 있으므로 버전별 스키마가 필요하다.

### 3.2 경쟁 사이트에서 확인한 유용한 기능

palworld.gg:

- 팰덱, 지도, 아이템/무기/방어구/건축/기술 데이터베이스
- 액티브·패시브·파트너 스킬
- 교배 계산기, 포획률 계산기, 팀 빌더
- 11개 언어만 노출되어 공식 17개 언어 전체 지원에는 미치지 못함

paldb.cc:

- Palpagos와 World Tree 지도
- 교배·IV·팰·포획 계산기
- 팰, 작업 적성, 스킬, 인벤토리, 건축, 기술, 상인, 던전 등 매우 넓은 데이터 범위
- 공식 17개 언어와 대응하는 언어 선택 제공
- 정보량은 많지만 초보자에게 메뉴와 수치가 과밀하고 “그래서 무엇을 하면 되는가”를 찾기 어렵다.

### 3.3 커뮤니티에서 반복되는 어려움

- 업데이트 후 교배 조합이 바뀌었는데 오래된 계산기가 잘못된 결과를 보여준다.
- 단순 최단 교배 경로가 실제로 가장 쉬운 경로는 아니다.
- 보유 팰과 보유 패시브를 일일이 입력하는 일이 번거롭다.
- 패시브, IV, 성별, 알 부화, 케이크, 응축을 하나의 목표로 연결하기 어렵다.
- 기지에서 어떤 작업 팰을 몇 마리 배치해야 생산량을 맞출 수 있는지 판단하기 어렵다.
- 지도에서 자원, 보스, 던전, 알 등을 함께 찾고 진행 상태를 기록하고 싶다.
- 서버 INI가 한 줄 형식이고 필드가 많아 오타, 범위 오류, 폐기 필드 사용이 잦다.
- 초반/복귀 유저는 데이터 자체보다 “현재 레벨에서 다음 할 일”을 원한다.

### 3.4 차별화 방향

- 모든 결과에 데이터 버전과 검증 상태 표시
- “최단”뿐 아니라 “실용성 높은” 교배 경로 제공
- 지도, 팰 상세, 아이템, 제작법, 계산기를 상호 연결
- 생산 목표에서 필요한 시설·작업 팰·원재료까지 역산
- 공식 문서 스키마 기반 서버 설정 생성 및 위험 경고
- 초보/복귀/엔드게임의 목표별 시작 화면
- 공식 17개 언어를 첫 공개 버전부터 동일 기능으로 지원

## 4. 대상 사용자와 주요 시나리오

### 초보자

- 현재 레벨과 목표를 선택해 다음 추천 기술, 장비, 포획 팰, 자원 위치를 확인
- 팰 이름을 몰라도 속성·외형·작업 적성·서식 지역으로 검색
- “채광 기지를 만들고 싶다”처럼 목적에서 필요한 팰과 시설로 이동

### 복귀 사용자

- 마지막 플레이 버전 이후 핵심 변경 사항 확인
- 기존 팰/교배 조합/서버 설정이 현재 버전에서 유효한지 검사
- 신규 지역·팰·기술·아이템만 필터링

### 최적화 사용자

- 보유 팰과 패시브를 기준으로 목표 팰까지의 현실적인 교배 계획 생성
- 기지 생산 병목과 작업 적성 조합 비교
- 팀의 속성, 파트너 스킬, 이동/운반/전투 역할 중복 확인

### 서버 운영자

- 공식 문서 기준으로 INI 생성, 기존 INI 붙여넣기 검사, 버전 마이그레이션
- 플레이 인원과 하드웨어 조건에 맞춘 성능 경고 확인
- 백업·RCON·REST API·PvP·크로스플레이 설정 체크리스트 확인

## 5. 정보 구조와 메뉴

### 5.1 데스크톱 주 메뉴

1. 홈
2. 지도
3. 팰
4. 계산기
5. 데이터베이스
6. 가이드
7. 서버 도구

우측 공통 도구:

- 전체 검색
- 게임 버전
- 언어
- 테마(시스템/라이트/다크)
- 내 목록

### 5.2 모바일 하단 메뉴

- 홈
- 지도
- 검색
- 계산기
- 더보기

### 5.3 전체 라우트 구조

```text
/{locale}/
/{locale}/map
/{locale}/map/palpagos
/{locale}/map/world-tree
/{locale}/pals
/{locale}/pals/{palSlug}
/{locale}/compare/pals
/{locale}/calculators
/{locale}/calculators/breeding
/{locale}/calculators/breeding-path
/{locale}/calculators/capture
/{locale}/calculators/iv
/{locale}/calculators/production
/{locale}/calculators/crafting
/{locale}/calculators/base
/{locale}/calculators/condensing
/{locale}/team-builder
/{locale}/database/items
/{locale}/database/recipes
/{locale}/database/technology
/{locale}/database/structures
/{locale}/database/skills
/{locale}/database/passives
/{locale}/database/npcs
/{locale}/database/npcs/{npcSlug}
/{locale}/guides
/{locale}/guides/getting-started
/{locale}/guides/returning-player
/{locale}/guides/base
/{locale}/guides/breeding
/{locale}/guides/combat
/{locale}/guides/server
/{locale}/server-tools/settings-generator
/{locale}/server-tools/settings-validator
/{locale}/server-tools/command-builder
/{locale}/updates
/{locale}/about
/{locale}/privacy
/{locale}/contact
```

계산기 상태는 `?target=...&parents=...`처럼 공유 가능한 쿼리로 보존하되 canonical URL과 sitemap에서는 제외한다.

## 6. 페이지별 상세 요구사항

### 6.1 홈

- “무엇을 하려는가?” 중심의 빠른 시작 카드
- 전체 검색: 현지화 이름, 공식 영문명, 별칭, ID, 카테고리, 가이드 키워드
- 지도·교배·생산·서버 설정 바로가기
- 현재 지원 게임 버전과 마지막 데이터 갱신일
- 신규/변경 데이터 요약
- 초보, 복귀, 엔드게임별 추천 흐름

### 6.2 인터랙티브 지도

필수 지도 레이어:

- 빠른 이동, 시작 지점, 지역/섬 경계
- 일반 팰 출현, 알파 팰, 보스, 탑, 레이드
- 던전 입구와 가능한 레벨/로테이션 정보
- 자원 노드, 광맥, 채집물
- 알, 보물상자, 메모, 수집 요소
- 상인, 암시장 상인, NPC, 캠프
- 기술/설계도/특수 아이템 관련 위치
- 낚시, 인양, 사건, 신규 버전 콘텐츠

사용성:

- 레이어 그룹, 전체 켜기/끄기, 검색, 범례
- 현재 확대 수준에 맞는 마커 클러스터링
- 마커 상세에서 관련 팰·아이템·가이드로 이동
- 좌표 표시와 URL 공유
- 방문/획득/즐겨찾기 상태를 로컬 저장
- “미획득만 보기”, “현재 레벨 이하만 보기”
- 지도별 독립 상태(Palpagos, World Tree)
- 키보드 조작, 스크린리더용 결과 목록 보기
- 모바일 하단 시트와 한 손 조작

정확성:

- 원본 좌표계 → 타일 좌표 변환을 고정된 기준점으로 검증
- 최소 20개 기준 위치를 게임 내 좌표/스크린샷과 대조
- 랜덤/시간/조건부 출현은 고정 위치처럼 표현하지 않고 조건을 표시

### 6.3 팰 데이터베이스와 상세

목록 필터:

- Paldeck 번호, 속성, 희귀도, 지역, 주/야간, 포획 가능 여부
- 작업 적성 종류/레벨
- 이동 유형, 탑승, 글라이더
- 기본 스탯 범주
- 드롭 아이템
- 신규/변경 버전

상세 정보:

- 공식 현지화 이름과 내부 ID
- 속성, 기본 스탯, 작업 적성, 식사량
- 파트너 스킬과 레벨별 효과
- 액티브 스킬, 패시브 관련 정보
- 서식지 지도, 알파/던전/레이드 출현
- 드롭과 획득처
- 교배값, 특수 조합, 목표/부모 계산기로 연결
- 기술 해금과 팰 장비
- 버전 변경 이력

비교:

- 최대 4마리
- 차이만 보기
- 작업/전투/이동 목적 프리셋
- 현지화된 표와 차트, 차트와 동일한 텍스트 표 제공

### 6.4 교배 계산기

모드:

1. 부모 두 마리 → 결과
2. 목표 팰 → 가능한 부모 조합
3. 보유 팰 → 만들 수 있는 팰
4. 보유 팰/패시브 → 목표까지 다단계 경로
5. 같은 종 개량 → 원하는 패시브/IV 계획

결과 정렬 기준:

- 번식 단계 수
- 시작 팰 포획 난이도
- 필요한 고유 시작 팰 수
- 성별 제약
- 희귀/보스/특수 팰 의존도
- 예상 알 수와 케이크 수
- 패시브 전달 가능성

결과에는 “최단 경로”와 “추천 실용 경로”를 구분해 보여준다. 실용 경로의 가중치는 사용자에게 공개하고 조절 가능하게 한다.

교배 계산식은 버전별 데이터 테이블을 우선한다. 일반 조합의 후보값은 다음 방식으로 검증하되 특수 조합이 항상 우선한다.

```text
targetBreedingPower = floor((parentAPower + parentBPower + 1) / 2)
offspring = targetBreedingPower에 가장 가까운 허용 팰
```

동률 처리, 특수 조합, 동일 종, 번식 불가 팰, 변종/돌연변이 규칙은 게임 파일에서 추출한 버전별 규칙 테이블로 처리한다. 위 일반식만으로 결과를 임의 생성하지 않는다.

그래프 알고리즘:

- 팰을 노드, 부모 조합→자식을 하이퍼엣지로 모델링
- 단계 수만 최소화할 때 BFS
- 현실성 가중치를 적용할 때 Dijkstra/A*
- 보유 패시브 전달 제약이 있으면 상태에 패시브 집합과 성별을 포함
- 동일 상태 중 더 비싼 경로 제거, 최대 탐색 깊이와 시간 제한 적용

### 6.5 포획률 계산기

입력:

- 대상 팰, 레벨, 현재 HP 비율, 상태 이상
- 사용 구체와 관련 모듈
- 플레이어 포획 보정
- 난이도/월드의 `PalCaptureRate`
- 후방 보너스 등 게임에서 확인된 조건

출력:

- 1회 성공률
- N회 안에 한 번 이상 성공할 확률
- 목표 성공 확률(50/75/90/95/99%)에 필요한 구체 수
- 구체별 효율 비교와 제작 원재료

확률 반복 계산:

```text
P(적어도 1회 성공) = 1 - (1 - p)^n
n = ceil(log(1 - 목표확률) / log(1 - p))
```

단일 시도 확률 `p`의 내부식은 추측하지 않는다. 해당 빌드의 게임 데이터/함수 상수와 실제 게임 표본으로 검증한 뒤 구현한다. 검증 전에는 “실험 기능”으로 제한한다.

### 6.6 IV 계산기

- 팰, 레벨, 표시 HP/공격/방어, 응축/영혼 강화/패시브/알파 여부 입력
- 가능한 IV 범위를 먼저 표시하고 값이 유일할 때 확정값 표시
- 반올림 때문에 여러 값이 가능한 경우 단일 숫자로 속이지 않음
- 스탯 계산 순서와 반올림 지점을 게임 빌드별 골든 테스트로 고정
- 입력 조합이 불가능하면 어떤 보정값이 잘못되었는지 설명

### 6.7 제작·생산 계산기

기능:

- 목표 아이템과 수량 → 전체 원재료 BOM
- 중간재 직접 제작/보유량/상인 구매 선택
- 재귀 제작 트리와 총합 표 동시 제공
- 시설, 필요 기술 레벨, 작업 종류 표시
- 예상 제작 시간과 필요한 작업 팰 수
- 다중 목표를 합친 장보기 목록

기본 계산:

```text
필요 공정 횟수 = ceil(목표 수량 / 1회 생산량)
직접 재료량 = 공정 횟수 × 1회 재료량
순 필요량 = max(0, 총 필요량 - 보유량)
총 원재료 = 제작 그래프를 말단 자원까지 재귀 전개 후 동일 항목 합산
```

제작 그래프의 순환을 자동 검사한다. 시간식은 게임 파일의 `workload`, 작업 적성별 작업량, 시설/패시브/월드 `WorkSpeedRate`의 실제 적용 순서가 확인된 뒤 버전별 함수로 구현한다.

### 6.8 기지 플래너

입력:

- 기지 목적: 채광, 농업, 식량, 케이크, 교배, 제작, 혼합
- 목표 생산량/시간
- 배치 가능 팰 수, 보유 팰, 야간 가동 여부
- 팰별 작업 적성, 패시브, 응축/강화
- 월드 작업 속도와 식사/SAN 조건

출력:

- 추천 시설 수와 팰 배치안
- 작업 적성별 수요/공급
- 운반, 전력, 냉각, 식량의 병목
- 예상 시간당 생산량
- 교대/야간 공백 경고
- 필요한 연구·기술·건축 재료
- 공유 가능한 배치 목록

초기 버전은 2D 건축 시뮬레이터가 아니라 “용량과 병목 계산”에 집중한다. 경로 탐색과 실제 동선은 수치가 검증되지 않으면 정량 예측하지 않는다.

### 6.9 응축·강화 계산기

- 현재 별/등급과 목표 등급 입력
- 필요한 동일 팰 수, 영혼, 기타 재료를 버전별 테이블로 계산
- 현재 보유량 차감
- 교배로 확보할 경우 필요한 알·케이크·시간 추정
- 잘못 알려진 누적/증분 수량을 방지하기 위해 두 값을 구분 표시

### 6.10 팀 빌더

- 최대 파티 인원 구성
- 속성 상성 커버리지
- 파트너 스킬 시너지/중복
- 전투, 이동, 채집, 포획 목적 프리셋
- 장비/탑승/글라이더 역할 중복 경고
- “최강” 단일 등급표 대신 목적과 조건을 명시
- 패치에 민감한 추천은 버전과 근거 규칙을 표시

### 6.11 서버 설정 생성기·검증기

입력 방식:

- 초보자용 단계별 폼
- 고급 사용자용 전체 파라미터
- 기존 `PalWorldSettings.ini` 붙여넣기/파일 로컬 읽기

출력:

- 완성된 INI 한 줄
- 변경점 diff
- 공식 기본값과의 차이
- Windows/Linux 경로와 적용 절차
- 서버 재시작/백업 체크리스트

검증:

- 공식 문서의 타입, 기본값, 최소/최대값, 지원 버전
- 오타, 중복 키, 폐기 키, 상충 설정
- `RCONEnabled`, REST API, 비밀번호, 공개 IP 등 보안 경고
- 높은 `BaseCampWorkerMaxNum`, 베이스 수, 드롭 수 등의 성능 경고
- PvP 필수 조합 검사
- `CrossplayPlatforms` 등 버전별 필드 마이그레이션

보안:

- 입력 파일은 서버로 업로드하지 않고 브라우저에서만 처리
- 비밀번호와 IP를 Analytics·URL·로컬 저장소에 기록하지 않음
- 예시 값과 실제 비밀값을 명확히 구분

### 6.12 가이드

가이드 유형:

- 첫 30분/레벨 구간별 진행
- 첫 기지, 두 번째 기지, 자동화 병목 해결
- 교배 입문부터 패시브·IV 최적화
- 포획 준비와 구체 선택
- 보스/레이드 준비 체크리스트
- 복귀 유저용 버전별 변경 요약
- 전용 서버 설치, 백업, 복원, 업데이트, 문제 해결

가이드는 데이터베이스 값을 복제하지 않고 팰/아이템/지도/계산기로 연결한다. 패치로 숫자가 바뀌면 본문 수동 수정 없이 데이터 참조로 갱신되게 한다.

### 6.13 주요 NPC 안내

- 데이터베이스 안에 상인과 비상인을 함께 찾는 통합 NPC 목록을 둔다. 상인은 별도 엔터티 종류가 아니라 NPC가 가진 거래 역할로 모델링한다.
- 공개 슬러그를 URL에 사용하고 게임 내부 캐릭터·블루프린트·대화 키는 공개 데이터와 화면에 노출하지 않는다.
- 상인은 판매 아이템 또는 팰 후보군, 가격, 묶음 수량, 전용 화폐, 1회 구매 제한처럼 게임 파일에서 검증된 거래 조건을 표시한다.
- 비상인은 수행 역할, 달성·요청 이벤트, 단계별 조건과 보상, 만나는 방법을 표시한다. 조건이나 보상이 연결되지 않은 NPC는 추측해서 게시하지 않는다.
- 모든 NPC 상세는 검증된 고정 위치를 지도와 연결하고, 상점·보상 항목은 아이템 상세로, 팰 요청·판매 후보는 팰 상세로 연결한다.
- 목록은 현지화 표시명과 역할로 검색·필터링하며, 통합 검색과 사이트맵에도 포함한다.
- 공식 현지화 명칭을 17개 지원 언어에 사용하고, 설명 문구는 동일 변경에서 전 언어를 완성한다.

## 7. 필요한 데이터 모델

핵심 엔터티:

- `GameBuild`: 버전, 플랫폼, 출시일, 데이터 해시
- `Pal`: 공식 ID, Paldeck 번호, 변종 관계, 속성, 희귀도
- `PalStats`: HP/공격/방어/작업 관련 기본값과 성장식
- `WorkSuitability`: 종류, 레벨, 보정
- `Skill`, `Passive`, `PartnerSkill`
- `BreedingRule`: 일반값, 특수 부모쌍, 제한, 우선순위
- `Item`, `Recipe`, `Technology`, `Structure`
- `Npc`, `NpcRole`, `NpcEncounter`, `NpcEvent`, `MerchantOffer`, `DropTable`, `Spawn`, `MapMarker`
- `CaptureRule`, `Sphere`, `StatusModifier`
- `ServerSetting`: 키, 타입, 기본값, 범위, 버전, 설명
- `PatchChange`: 추가/변경/삭제와 영향 엔터티
- `Localization`: locale, key, text, provenance
- `Provenance`: 비공개 출처, 추출 도구, 버전, 검증 상태

모든 엔터티는 사람이 바뀌기 쉬운 표시명 대신 불변 공식 ID로 연결한다.

## 8. 데이터 수집·검증 파이프라인

### 8.1 출처 우선순위

1. 사용자가 합법적으로 설치한 Palworld 게임 파일
2. Pocketpair 공식 사이트, 공식 패치 노트, 공식 서버 문서, 공식 스토어/뉴스
3. palworld.gg, paldb.cc 및 신뢰 가능한 커뮤니티 자료

커뮤니티 자료가 공식/게임 파일과 다르면 커뮤니티 값을 채택하지 않는다. 게임 파일만으로 의미를 해석하기 어려운 필드는 실제 게임 내 표본이나 별도 신뢰 자료로 교차 검증한다.

### 8.2 내부 수집 명세

공개 빌드에서 제외되는 private manifest에 다음을 기록한다.

- source type
- 로컬 상대 경로 또는 URL
- 게임 빌드
- 수집일
- 추출 도구/버전
- 변환 단계
- 라이선스/이용 조건 메모
- 검증 상태와 검증자

게임 설치 전체, 세이브 파일, 키, 계정 정보는 수집하거나 배포하지 않는다. DRM·암호화·접근 제어를 우회하지 않는다.

### 8.3 자동 검증

- ID 중복과 참조 무결성
- 모든 레시피 재료와 결과 존재 여부
- 교배 조합 대칭성 및 특수 규칙 우선순위
- 지도 좌표 범위와 중복 마커
- 숫자 타입, 허용 범위, 단위
- 번역 키/placeholder/마크업 일치
- 이전 빌드 대비 비정상 대량 변경 감지
- 대표 계산 사례 골든 테스트
- 생성 데이터 해시와 결정적 빌드

### 8.4 패치 대응 절차

1. 공식 패치 노트 감지
2. 새 게임 빌드 확보 후 추출
3. 구조/수치 diff 생성
4. 자동 검증과 변경 영향 페이지 목록 생성
5. 계산식 골든 테스트
6. 주요 변경 수동 확인
7. 프리뷰 배포
8. 승인 후 프로덕션 반영

“패치 노트에 언급 없음”을 “값 변경 없음”으로 간주하지 않는다.

## 9. 다국어 계획

### 9.1 지원 로케일

| 게임 지원 언어 | 사이트 locale |
|---|---|
| English | `en-US` |
| Simplified Chinese | `zh-CN` |
| Traditional Chinese | `zh-TW` |
| Japanese | `ja-JP` |
| French | `fr-FR` |
| Italian | `it-IT` |
| German | `de-DE` |
| Spanish - Spain | `es-ES` |
| Portuguese - Brazil | `pt-BR` |
| Russian | `ru-RU` |
| Korean | `ko-KR` |
| Indonesian | `id-ID` |
| Spanish - Latin America | `es-419` |
| Thai | `th-TH` |
| Turkish | `tr-TR` |
| Vietnamese | `vi-VN` |
| Polish | `pl-PL` |

### 9.2 언어 선택 규칙

1. URL의 locale
2. 사용자가 직접 저장한 선택
3. `en-US`

브라우저/운영체제 언어로 최초 언어를 자동 선택하지 않는다. 루트 `/`는 `/en-US/`로 이동한다.

### 9.3 번역 원칙

- 팰·아이템·스킬·기술명은 추출한 게임 공식 현지화가 최우선
- 공식 번역이 없는 사이트 UI/설명문만 GPT로 번역
- 번역 provenance를 `official`, `gpt`, `reviewed`로 기록
- GPT는 게임 수치·사실·조합을 만들지 않음
- 변수, ID, 숫자, 단위, HTML/Markdown 구조를 자동 검증
- 페이지 제목, 설명, 접근성 라벨, 오류, 빈 상태, 광고/동의 문구까지 전부 번역
- 현지화 이름 정렬은 `Intl.Collator(activeLocale)` 사용
- 기술 트리/등급/진행 순서는 유지하고 같은 그룹 안에서만 이름순 정렬

### 9.4 번역 QA

- 17개 locale 누락 0건
- CJK, 키릴, 태국, 베트남 문자 폰트 확인
- 긴 독일어/러시아어 레이아웃
- 스페인어 두 변형과 중국어 두 변형 혼용 방지
- 숫자, 날짜, 퍼센트, 단위 locale 포맷
- 런타임 영어 fallback로 누락을 숨기지 않음

## 10. 검색 설계

인덱싱 대상:

- 활성 언어 표시명
- 공식 영어명
- 공식 ID와 Paldeck 번호
- 안전한 별칭
- 속성, 작업 적성, 아이템 종류
- 레시피 재료/결과
- 지도 마커 종류
- 가이드 제목과 키워드

동작:

- Unicode, 대소문자, 공백, 안전한 구두점 정규화
- 언어 규칙에 맞는 범위에서만 발음기호 무시
- 잘못된 결과를 만드는 과도한 fuzzy/transliteration 금지
- 검색 결과 수, 활성 필터, 초기화, 빈 상태 제공
- 큰 인덱스는 페이지별 분할하고 필요 시 Web Worker 사용
- 검색어는 분석 도구로 보내지 않음

## 11. 디자인 스타일

### 11.1 콘셉트

“탐험가의 현장 도구 + 깨끗한 데이터 대시보드”

- 게임 UI를 복제하지 않고 원본성이 있는 시각 체계
- 자연/섬을 연상하는 청록색 계열을 주 강조색으로 사용
- 희귀도와 속성 색은 보조 수단이며 항상 아이콘/텍스트 병행
- 둥근 카드보다 정보 밀도가 필요한 표·필터·계산 결과를 우선
- 배경 장식은 지도와 수치 가독성을 방해하지 않음

### 11.2 테마

- `system`이 최초 기본
- 사용자가 light/dark/system을 직접 선택하면 저장
- 첫 화면 표시 전에 테마 적용해 깜박임 방지
- canvas, surface, text, border, accent, focus, success, warning, error, chart, ad 영역을 의미 토큰으로 정의
- 두 테마 모두 WCAG 2.2 AA 대비 검증

### 11.3 반응형과 접근성

- 기준 폭: 360, 768, 1024, 1440px
- 터치 대상 최소 44×44 CSS px
- 모든 필터/지도/계산기 키보드 사용 가능
- 명확한 heading/label/table 구조
- 포커스 표시, 상태를 색만으로 전달하지 않음
- 지도는 동등한 결과 목록 제공
- 애니메이션 감소 설정 존중

## 12. 기술 아키텍처

권장 구성:

- TypeScript 기반 정적 사이트 생성 프레임워크
- 정적 HTML을 기본으로 하고 지도/계산기만 선택적으로 hydration
- 데이터 추출기와 웹 앱을 분리한 workspace
- 게임 중립 공유 코드와 Palworld 전용 설정/데이터 분리
- 정적 JSON 또는 압축된 페이지별 데이터 청크
- 지도 타일과 이미지의 WebP/AVIF 변환 및 크기별 제공
- Cloudflare Pages에는 정적 산출물만 배포

예시 구조:

```text
apps/web
packages/core
packages/search
packages/calculators
games/palworld/config
games/palworld/data/raw-private
games/palworld/data/generated
games/palworld/locales
games/palworld/assets
scripts/extract
scripts/validate
private/provenance
tests/golden
```

`raw-private`, provenance, 설치 경로, 원본 추출 로그는 프로덕션 산출물과 sitemap에 포함하지 않는다.

성능 목표:

- 주요 정적 페이지 모바일 LCP ≤ 2.5초
- CLS ≤ 0.1
- INP ≤ 200ms
- 검색/필터 100ms 이내 반응을 목표
- 지도 초기 로드에 전체 마커/이미지를 한 번에 싣지 않음

## 13. SEO

- 모든 공개 페이지에 locale별 고유 title/description/h1
- `/{locale}/...` canonical
- 17개 `hreflang`과 `x-default → en-US`
- locale별 또는 sitemap index 기반 sitemap
- 계산기 입력, 검색/필터, 추적 파라미터는 canonical/sitemap 제외
- 팰·아이템·가이드 사이 crawlable 링크
- 보이는 검증 정보와 일치하는 Breadcrumb/Article 등 구조화 데이터
- robots.txt, canonical, sitemap, Open Graph 도메인 일치
- 자동 생성된 얇은 조합 페이지를 무제한 색인하지 않음

## 14. Analytics, Search Console, AdSense

### 14.1 Analytics

사이트 전용 GA4 property와 web stream을 사용한다. 이 프로젝트의 확정 Measurement ID는 `G-FF7N186M72`이며 Palworld 전용 공개 설정에서만 주입한다. 환경 변수로 재정의할 수 있더라도 production 빌드와 배포 설정은 이 값과 일치해야 하며, 이전 프로젝트나 폐기된 Measurement ID가 남아 있으면 출시를 중단한다.

Analytics 설정 확인·수정 작업계획:

1. 소스 설정, `.env` 예시, Cloudflare Pages 환경 변수, 빌드 스크립트에서 실제 적용되는 Measurement ID를 전수 검색해 `G-FF7N186M72`로 통일한다.
2. production 빌드 산출물에서 GA 로더 URL과 `gtag('config', ...)`가 동일한 ID를 사용하는지 확인하고, 다른 `G-` ID가 포함되지 않았는지 자동 검사한다.
3. 사용자가 동의하기 전에는 GA 스크립트·쿠키·이벤트가 생성되지 않고, 거부 시에도 로드되지 않는지 브라우저에서 확인한다.
4. SPA 자동 page view는 끄고, 실제 경로 변경당 `page_view`가 한 번만 발생하는지와 동의 직후 현재 페이지가 최대 한 번 기록되는지 확인한다.
5. DebugView 또는 Tag Assistant로 `page_view`, `tool_open`, `calculation_complete`, `map_layer_toggle`, `filter_apply`, `outbound_contact`의 수신 여부와 허용된 파라미터만 포함되는지 확인한다.
6. 검색 원문, 지도 좌표·핀 이름, 서버명·IP·비밀번호·INI 내용, 보유 수량, 메모 등 자유 입력값이 이벤트·URL·로그로 전송되지 않도록 회귀 테스트를 추가한다.
7. production 도메인의 데이터 스트림 URL이 `https://palworld-helper.woofy.blog`인지 GA 관리 화면에서 확인하고, 다른 도메인 또는 중복 태그가 연결되어 있으면 수정한다.

완료 조건: production 산출물과 실제 사이트가 `G-FF7N186M72` 하나만 사용하고, 동의 전송 차단·SPA 중복 방지·민감 입력 제외가 자동 테스트와 브라우저 네트워크 검사에서 모두 통과한다.

허용 이벤트 예:

- `tool_open`
- `calculation_complete`(도구 종류만)
- `map_layer_toggle`(레이어 ID만)
- `filter_apply`(필터 종류만)
- `outbound_contact`

금지:

- 원문 검색어
- 팰 별명 같은 자유 입력
- 서버명, IP, 비밀번호, INI 내용
- 세이브 파일 정보
- 사용자 식별 가능한 값

locale 경로 전환 시 중복 page_view가 발생하지 않도록 초기화를 한 곳에서 관리한다.

### 14.2 Search Console

- 도메인 확정 후 별도 property 생성
- 제공받은 검증 토큰을 사이트 전용 설정 또는 정확한 검증 파일에 추가
- 배포 후 sitemap 제출
- locale별 색인, canonical, soft-404, Core Web Vitals 점검
- 다른 게임 사이트의 토큰을 재사용하지 않음

### 14.3 AdSense

- 사이트 전용 publisher ID와 placement별 slot ID 사용
- production에서만 로드
- `ads.txt`는 승인된 정확한 계정 행만 추가
- 광고 공간을 미리 예약해 CLS 방지
- 지도 조작, 계산 입력, 결과 버튼 주변에는 광고 배치 금지
- 동의가 필요한 지역에서는 동의 전 저장/개인화 광고 금지
- 첫 출시 광고 위치 후보: 홈 콘텐츠 중간, 목록 페이지 카드 사이가 아닌 섹션 사이, 상세 본문 하단

## 15. 테스트와 출시 게이트

### 데이터

- 현재 지원 빌드와 데이터 빌드 일치
- 모든 ID/참조/레시피/교배 조합 검증 통과
- 공식 용어 우선순위 확인
- 잘못된 값 0건, 미확인 값은 명시
- private manifest와 설치 경로가 빌드에 없음

### 계산기

- 게임 내 확인 사례와 골든 테스트 일치
- 경계값, 반올림, 동률, 특수 조합
- 버전이 다른 데이터 혼용 방지
- 공유 URL 복원과 잘못된 파라미터 처리

### 다국어

- 17개 locale 빌드 성공
- 누락 키/placeholder/markup 오류 0건
- hreflang/canonical/sitemap 일치
- 공식 현지화 명칭 샘플 대조
- locale 정렬의 결정성

### UI/접근성

- 모바일/태블릿/데스크톱
- light/dark/system 및 초기 깜박임
- 키보드, 포커스, 스크린리더 이름
- 지도 대체 목록
- 광고 컨테이너가 조작을 가리지 않음

### 품질/배포

- lint, typecheck, unit, integration, E2E, production build
- 깨진 링크와 이미지 검사
- 비밀/로컬 경로/출처 로그 유출 검사
- 정적 산출물만 Cloudflare Pages에 포함
- 프리뷰 검수 후에만 production 배포

## 16. 단계별 작업 일정

일정은 1명의 전담 개발 기준 대략치이며, 데이터 추출 난이도와 17개 언어 리뷰 인력에 따라 달라진다.

### Phase 0 — 발견·권리·기준선 (3~5일)

- 공식 빌드/언어/플랫폼 범위 확정
- 설치 파일 구조와 추출 가능 범위 조사
- 이미지와 데이터 이용 조건 검토
- 경쟁 사이트 기능표와 사용자 문제 목록 확정
- 도메인, 브랜드, 지원 연락처 placeholder 정의

완료 조건: 데이터 획득 계획과 사용 가능한 자산 범위가 문서화됨.

### Phase 1 — 기반과 데이터 파이프라인 (1.5~2주)

- 프로젝트/빌드/테스트 구조
- Palworld 전용 설정
- 추출→정규화→검증→생성 파이프라인
- private provenance manifest
- 17개 locale 구조와 공식 게임 용어 추출
- 버전 diff와 데이터 해시

완료 조건: 팰/아이템/레시피/교배/서버 설정의 대표 데이터가 재현 가능하게 생성됨.

### Phase 2 — 디자인 시스템·공통 검색·SEO (1~1.5주)

- 반응형 shell, 메뉴, 테마
- locale routing과 선택 규칙
- 공통 검색
- SEO metadata, hreflang, sitemap, robots
- 접근성 기반 컴포넌트

완료 조건: 17개 locale의 기본 페이지가 정적 생성되고 검색/테마가 검증됨.

### Phase 3 — MVP 필수 기능 (3~4주)

- Palpagos/World Tree 지도
- 팰 목록/상세/비교
- 교배 기본/역산/경로 계산기
- 아이템/레시피/기술/구조물 DB
- 제작 BOM 계산기
- 서버 설정 생성기/검증기

완료 조건: 지도·계산기·정보라는 필수 범위가 모두 실제 데이터로 동작함.

### Phase 4 — 고급 도구 (2~3주)

- 포획률, IV, 생산, 기지, 응축 계산기
- 팀 빌더
- 보유 팰/즐겨찾기/체크리스트
- 버전 변경 비교

완료 조건: 각 계산기의 게임 내 대조 사례와 골든 테스트가 통과함.

### Phase 5 — 콘텐츠·17개 언어 QA (2~3주)

- 초보/복귀/교배/기지/서버 가이드
- GPT 보완 번역 생성 및 provenance
- 중요 메뉴/SEO/개인정보/광고 문구 사람 리뷰
- 모든 스크립트와 긴 문장 레이아웃 QA

완료 조건: 런타임 fallback 없이 17개 언어 콘텐츠가 완결됨.

### Phase 6 — 측정·수익화·출시 (1주)

- GA4 `G-FF7N186M72`와 consent 설정 검증·수정
- production 산출물, Tag Assistant/DebugView, SPA 중복 page view, 자유 입력 제외 회귀 테스트
- Search Console 검증(ID/파일 제공 시)
- AdSense, ads.txt, 광고 자리(ID 제공 및 승인 시)
- Cloudflare Pages preview
- 성능/접근성/SEO/보안 최종 점검
- production 배포

완료 조건: 모든 출시 게이트 통과 및 운영 체크리스트 승인.

### 전체 예상

- 실사용 가능한 MVP: 약 6~9주
- 17개 언어와 고급 계산기까지 포함한 정식 범위: 약 11~15주
- 사람 번역 리뷰가 모든 언어에 필요한 경우 별도 병렬 일정 필요

## 17. 우선순위

### P0 — 첫 공개에 반드시 포함

- 17개 언어 기본 지원
- 전체 검색
- Palpagos/World Tree 지도
- 팰 DB
- 교배 계산기
- 제작/재료 계산기
- 아이템/레시피/기술 DB
- 서버 설정 생성/검증
- 버전·갱신일·정확성 표시
- light/dark/system
- SEO 기본

### P1 — 첫 공개 직후

- 생산/기지/응축 계산기
- 포획률/IV 계산기
- 팰 비교와 팀 빌더
- 진행도 체크리스트
- 복귀 유저 변경 비교

### P2 — 데이터 검증 후 확장

- 세이브 파일의 브라우저 로컬 분석
- 더 정교한 교배 패시브 확률 최적화
- 기지 배치 시뮬레이션
- 사용자 정의 목록 export/import

세이브 분석은 파일 형식, 개인정보, 서버 세이브 권한, 패치 호환성을 별도 검토하기 전에는 P0에 넣지 않는다.

## 18. 운영 계획

- 공식 패치 발표 모니터링
- 패치마다 데이터 diff와 영향 계산기 자동 목록
- 오류 제보에는 게임 버전, 플랫폼, 엔터티 ID, 기대/실제 값만 받도록 폼 구성
- 자유 입력을 Analytics와 분리
- 긴급 오류 시 해당 계산기만 “검증 중” 상태로 전환
- 월 1회 깨진 링크, Search Console, Core Web Vitals, 광고 정책 점검
- 분기별 오래된 가이드와 GPT 번역 리뷰

## 19. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| 패치로 교배/수치 변경 | 빌드별 데이터 분리, 자동 diff, 최신성 배지 |
| 커뮤니티 수치 오류 | 게임 파일/공식 자료 재검증 전 미채택 |
| 게임 파일 재배포 문제 | 필요한 값만 정규화, 권리 검토, private manifest |
| 17개 언어 품질 편차 | 공식 용어 우선, provenance, placeholder QA, 중요 문구 리뷰 |
| 지도 좌표 오류 | 기준 위치 대조와 버전별 좌표셋 |
| 계산식 반올림 오류 | 게임 내 골든 케이스와 경계값 테스트 |
| 모바일 데이터 과부하 | 데이터 분할, 지연 로드, Web Worker, 클러스터링 |
| AdSense로 UX 악화 | 조작부와 분리, 공간 예약, 밀도 제한 |
| 서버 비밀 유출 | 전부 로컬 처리, URL/로그/Analytics 전송 금지 |

## 20. 착수 전에 필요한 결정과 제공 항목

- 최종 사이트명과 도메인
- 로고/브랜드 방향 또는 신규 제작 여부
- 지원/문의 주소
- 합법적으로 설치된 게임 파일을 데이터 추출에 사용할 수 있는지
- 우선 지원 플랫폼 범위(Steam/Xbox/PS5/Mac 차이)
- Cloudflare 계정/프로젝트(배포 시점)
- GA4 Measurement ID: `G-FF7N186M72`(확정, production 데이터 스트림·도메인 연결 상태 확인 필요)
- Search Console 검증값 또는 파일(나중에 제공 가능)
- AdSense publisher/slot ID와 ads.txt 행(승인 후)
- 쿠키/광고 동의 적용 대상 지역과 법무 문구

## 21. 참고 자료

공식/권위 자료:

- Palworld Steam 상점 — 지원 언어와 제품 범위: https://store.steampowered.com/app/1623730/Palworld/
- Palworld 공식 서버 가이드: https://docs.palworldgame.com/
- 서버 실행 인자: https://docs.palworldgame.com/settings-and-operation/arguments/
- 서버 설정 파라미터: https://docs.palworldgame.com/settings-and-operation/configuration/
- 서버 REST API 설정 스키마: https://docs.palworldgame.com/1.0.0/api/rest-api/settings/
- 공식 Palworld 사이트: https://www.palworldgame.com/

기능 벤치마크/교차 확인:

- palworld.gg: https://palworld.gg/
- palworld.gg 지도: https://palworld.gg/map
- palworld.gg 교배 계산기: https://palworld.gg/breeding-calculator
- paldb.cc: https://paldb.cc/
- paldb.cc 지도: https://paldb.cc/en/Map
- paldb.cc 교배 도구: https://paldb.cc/en/Breed

커뮤니티 자료는 사용자 어려움과 기능 수요 파악에만 사용하며, 게시할 게임 사실의 최종 근거로 단독 사용하지 않는다.
